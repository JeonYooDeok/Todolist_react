# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on:
  push:
    branches:
      - main

jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run : |        
        echo "VITE_API_KEY=${{ secrets.VITE_API_KEY }}" >> .env
        echo "VITE_API_KEY=${{ secrets.VITE_AUTH_DOMAIN }}" >> .env
        echo "VITE_API_KEY=${{ secrets.VITE_PROJECT_ID }}" >> .env
        echo "VITE_API_KEY=${{ secrets.VITE_STORAGE_BUCKET }}" >> .env
        echo "VITE_API_KEY=${{ secrets.VITE_APP_ID }}" >> .env 
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_TODOLIST_REACT_5CCA0 }}"
          projectId: todolist-react-5cca0
          channelId: live
